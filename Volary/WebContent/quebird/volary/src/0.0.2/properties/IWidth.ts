module Volary {

    /**
     * Width specification.
     */
    export interface IWidth {
        /**
         * Retrieves the width.
         */
        getWidth() : number;
        /**
         * Sets the width.
         */
        setWidth(width: number);
    }

}